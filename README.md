# FLEXScript Specification
FLEXScript is a programming language specification for clean and concise code, with syntax based off already defined technologies, such as `Javascript`, `JScript` and `ECMAScript`.
